import AdminLayout from "../../../hoc/adminLayout";

const Profile = (props) => {
    return (
        <AdminLayout section="Profile">
            Profile
        </AdminLayout>
    );
};

export default Profile;
